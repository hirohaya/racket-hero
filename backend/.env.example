# .env.example - Template de Variáveis de Ambiente
# Copie este arquivo para .env e preencha com seus valores

# ============================================================================
# JWT Configuration
# ============================================================================
JWT_SECRET_KEY=seu-secret-super-seguro-minimo-32-caracteres-aleatorio
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================================================
# Database
# ============================================================================
# SQLite (recomendado para MVP)
DATABASE_URL=sqlite:///./racket_hero.db

# PostgreSQL (para produção, mais tarde)
# DATABASE_URL=postgresql://user:password@localhost/racket_hero

# ============================================================================
# Email Configuration (opcional para MVP v1.0)
# ============================================================================
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASSWORD=seu-app-password

# Para Gmail:
# 1. Ativar 2FA em https://myaccount.google.com/security
# 2. Gerar app password em https://myaccount.google.com/apppasswords
# 3. Copiar app password aqui

# ============================================================================
# Frontend Configuration
# ============================================================================
# Adicionar em frontend/.env.local (não no .env do backend)
# REACT_APP_API_URL=http://localhost:8000

# ============================================================================
# Notas Importantes
# ============================================================================
# - Nunca commitar .env no GitHub (está em .gitignore)
# - Manter .env.example como template
# - Em produção, usar variáveis de ambiente do servidor (Heroku, DigitalOcean, etc)
# - JWT_SECRET_KEY deve ser criptograficamente seguro (mín. 32 caracteres aleatórios)
